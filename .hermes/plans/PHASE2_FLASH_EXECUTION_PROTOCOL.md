# PHASE 2: Execution Protocol for 3.7 Flash

> **CRITICAL DIRECTIVE TO EXECUTING AGENT (Gemini 3.7 Flash):**
> You are the executor, but you are not allowed to be a blind code-monkey. Aryan specifically requested that you **THINK, TEST, and RESEARCH** rigorously before modifying files.

## Mandatory Execution Loop for Every Component:
1. **Research First:** Before touching UI, check the existing CSS tokens in `app/globals.css`. 
2. **Copywriting constraint:** NO "AI-sounding" word salads (e.g., "interactive architectures", "verified network topologies"). Speak like an experienced, human agency founder talking to a business owner.
3. **Responsive constraint:** Every CSS change must include an explicitly tested mobile viewport state (`@media (max-width: 768px)` and `(max-width: 480px)`). 
4. **Visual Testing:** You must use `browser_exec` to capture a screenshot after every major UI block is updated.

If you don't understand the stylistic goal, pause and search `.hermes/plans/DESIGN_DNA.md` first.
